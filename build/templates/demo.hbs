<h2><span><a href="../index.html">JS Comp</a> &gt; </span>{{folderName}}</h2>

<h4>Live</h4>

<div id="app"></div>
<script src="scripts/{{bundleName}}"></script>

<h4>Implementations</h4>

To see a specific implementation of this demo, click the description links in the `implementation` column below! The app running above is one of these implementations picked at random.

<table>
  <thead>
    <th>Framework</th><th>Implementation</th><th>Code size</th><th>Bundle size</th><th>Author</th>
  </thead>
  <tbody>
    {{#each frameworks}}
      {{#each implementations}}
        <tr>
          <td>{{niceFrameworkName}} ({{frameworkVersion}})</td>
          <td><a href="{{url}}">{{title}}</a></td>
          <td>{{size}}</td><td>{{bundleSize}}</td>
          <td><a href="http://github.com/{{author}}" target="_blank">{{author}}</a></td>
        </tr>
      {{/each}}
    {{/each}}
  </tbody>
</table>

{{{description}}}